package Model.Cards;

import Model.Card;
import Model.Effect;
import Model.Enemy;
import Model.*;

import java.util.ArrayList;

public class Bash extends Card {

    /*
    Deal 8(10) damage. Apply 2(3) Vulnerable.
     */
    public ArrayList<Effect> getEffects(Enemy e){
        ArrayList<Effect> effects;

        if( u)
        Effect effect = new Damage()
        return null;
    }
}
